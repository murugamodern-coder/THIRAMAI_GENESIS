[pytest]
pythonpath = .
testpaths = tests
# importlib avoids namespace collisions; capture=no avoids Py3.14/Windows teardown issues in some CI hosts.
addopts = --import-mode=importlib --capture=no
filterwarnings =
    ignore::DeprecationWarning
