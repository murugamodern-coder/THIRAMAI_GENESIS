# Canonical SLO Prometheus rules live under ../prometheus/
#   - recording-rules.yml
#   - alert-rules.yml
#
# This file is intentionally empty of rule groups to avoid duplicate alerts if both
# paths were loaded historically. Point rule_files in prometheus.yml at ../prometheus/*.yml.
groups: []
