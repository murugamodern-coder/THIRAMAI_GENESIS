# Production ASGI stack (Gunicorn + Uvicorn workers) and error reporting.
-r requirements-base.txt
uvicorn[standard]==0.44.0
gunicorn==25.3.0
sentry-sdk[fastapi]==2.58.0
prometheus-fastapi-instrumentator==7.1.0
rq==1.16.2

# AWS Secrets Manager backend for core/secrets_manager.py (optional at runtime if SECRETS_BACKEND=environment).
boto3>=1.34.0,<2

# Live broker SDKs
fyers-apiv3>=3.1.0,<4
kiteconnect>=5.0.0,<6

# psutil powers the process-level CPU / memory gauges in
# services/observability/business_metrics.py. The module degrades gracefully
# when this package is missing, so it is "soft optional" — installed in prod
# only.
psutil>=5.9.0,<6

# OpenTelemetry distributed tracing — fully optional. Enabled at runtime via
# THIRAMAI_TRACING_ENABLED=1; core/tracing.py is a no-op when these are
# absent. Uncomment the block below in environments that ship an OTLP
# collector (Tempo / Jaeger / Otel-Collector).
#
# opentelemetry-api>=1.27.0,<2
# opentelemetry-sdk>=1.27.0,<2
# opentelemetry-exporter-otlp-proto-grpc>=1.27.0,<2
# opentelemetry-instrumentation-fastapi>=0.48b0,<1
# opentelemetry-instrumentation-sqlalchemy>=0.48b0,<1
# opentelemetry-instrumentation-redis>=0.48b0,<1
