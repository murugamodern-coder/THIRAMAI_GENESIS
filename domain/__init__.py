"""
Domain layer — pure business rules and invariants.

Implementation today lives mainly under ``services/`` and ``core/``; this package marks the boundary
for gradual extraction (tenant invariants, plan rules) without new product features.
"""
