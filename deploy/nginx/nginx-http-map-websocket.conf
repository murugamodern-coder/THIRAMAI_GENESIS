# WebSocket upgrade map — paste ONCE inside the http { } block of /etc/nginx/nginx.conf
# (before sites-enabled includes), unless an identical map already exists.
#
# map $http_upgrade $connection_upgrade {
#     default upgrade;
#     ''      close;
# }
