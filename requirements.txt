# Convenience install: production stack + dev/test tools (pinned).
# Production Docker image installs only requirements-base + requirements-production (see Dockerfile).
-r requirements-production.txt
-r requirements-dev.txt
