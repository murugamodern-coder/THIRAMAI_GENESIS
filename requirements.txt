# THIRAMAI Genesis — runtime stack (FastAPI / async / ASGI)
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
jinja2>=3.1.0
python-dotenv>=1.0.0
pyyaml>=6.0.0
sqlalchemy>=2.0.0
alembic>=1.14.0
sqlparse>=0.5.0
psycopg2-binary>=2.9.9
apscheduler>=3.10.0
groq>=0.11.0
# Multi-agent orchestrator swarm (LangGraph blackboard); optional — THIRAMAI_ORCHESTRATOR_SWARM=1
langgraph>=0.2.0
langchain-core>=0.3.0
tavily-python>=0.5.0
pypdf>=4.0.0
fpdf2>=2.8.0
# pydantic: pulled in by fastapi
pydantic-settings>=2.0.0
# httpx: optional client tests / some Tavily paths; uvicorn[standard] covers websockets — no separate WebSocket extra required unless you add WS routes
httpx>=0.27.0
numpy>=1.26.0
# Research Hub: Indian equity fuzzy match + live quotes (optional Alpha Vantage via THIRAMAI_ALPHA_VANTAGE_API_KEY)
rapidfuzz>=3.9.0
yfinance>=0.2.40

# Auth / RBAC (JWT + bcrypt + OAuth2 form login)
python-jose[cryptography]>=3.3.0
bcrypt>=4.0.1
cryptography>=42.0.0
python-multipart>=0.0.9
email-validator>=2.0.0
redis>=5.0.0

# Docker SDK — micro-kernel sandbox (optional; set THIRAMAI_KERNEL_SANDBOX=1)
docker>=7.0.0

# Vector LTM (optional; THIRAMAI_LTM_ENABLED=1) — Chroma persistent store under var/chroma_ltm
chromadb>=0.5.0

# Stage 5 World Scanner — optional JS-heavy pages (THIRAMAI_WORLD_SCAN_PLAYWRIGHT=1):
#   pip install playwright && playwright install chromium

# Load testing (optional): locust -f locustfile.py
locust>=2.20.0

# Rate limiting (optional alternative to custom middleware); not wired in app by default
slowapi>=0.1.9

# Tests (CI / local): pip install -r requirements.txt && pytest tests/
pytest>=8.0.0

# Notes (Phase 7 audit):
# - Cash Flow Radar SVG is rendered in the browser (index.html); no Python SVG/matplotlib dependency.
# - factory/market_watch.py 90-day HDPE series uses stdlib datetime + hash only — pandas/matplotlib NOT required.
# - services/predictive_engine.py uses numpy for monthly revenue / production forecasts.
